"""Optional format tracks.

None of this is needed to complete the core tutorial. Each module here provides a
converter, which turns the generated dataset into that format, and a
:class:`dataaware.adapters.DatasetAdapter`, which reads it back.

Optional dependencies are imported inside the functions that need them, so importing
this package costs nothing and the core tutorial runs with none of them installed.
"""
